require('dotenv').config(); const { GoogleGenerativeAI } = require('@google/generative-ai'); async function find() { const key = process.env.GEMINI_API_KEY; if (!key) return; const genAI = new GoogleGenerativeAI(key); try { const list = await genAI.listModels(); console.log('MODELS:'); list.models.forEach(m => console.log(m.name.replace('models/', ''))); } catch(e) { console.error(e.message); } } find();